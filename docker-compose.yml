services:
  monitor:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      # Persist dedup state on the mounted volume so restarts don't re-notify.
      STATE_PATH: /data/state.json
    volumes:
      - ./data:/data
